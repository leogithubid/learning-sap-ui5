//scaffolding template
sap.ui.define(
    ["sap/ui/core/mvc/Controller"],//load dependencies
    function(oController){//the dependencies pass the objects to this
        return oController.extend("home.controller.main");//extend controller
    });